; Uploads: ticket screenshots of 5 MB and news videos of 50 MB, several per request, plus the form fields.
; Read by PHP-FPM and CGI deployments; Apache with mod_php reads the same limits from .htaccess.
upload_max_filesize = 50M
post_max_size = 120M
