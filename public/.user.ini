; Ticket screenshots: up to five images of 5 MB each per request, plus the form fields.
; Read by PHP-FPM and CGI deployments; Apache with mod_php reads the same limits from .htaccess.
upload_max_filesize = 5M
post_max_size = 30M
